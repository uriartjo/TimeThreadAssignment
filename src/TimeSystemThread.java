public class TimeSystemThread {
    /* Have this class extend the Thread class
       The run method should output the current date and time at 10 second intervals
        The method should run indefinitely
     */
}
